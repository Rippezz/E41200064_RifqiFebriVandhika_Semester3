<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.py7.appbiodata">

    <application
        android:allowBackup="true"
        android:icon="@drawable/data"
        android:label="@string/app_name"
        android:roundIcon="@drawable/data"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".MainActivity"
            android:label="E41200064_SqLite"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        //memberikan aksi untuk tambah dan edit data
        <activity android:name=".AddActivity"
            android:label="Tambah Data"/>
        <activity android:name=".EditActivity"
            android:label="Edit Data"></activity>
    </application>

</manifest>